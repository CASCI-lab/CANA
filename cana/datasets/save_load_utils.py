"""
The goal of this file is to speed up the processing of networks so that we don't do duplicative computational work.
"""
import numpy as np
def saveNetwork(N):
    file = N.filepath.strip(".txt").strip(".cnet")
    

def loadNetwork(NetworkName):